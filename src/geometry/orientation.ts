/**
 * @fileoverview Enum defining the four cardinal directions for character orientation.
 * Used throughout the game for character movement, animations, and direction-based logic.
 */

/**
 * Enum representing the four cardinal directions.
 * Used for character orientation, movement, and animations.
 * 
 * @enum {string}
 */
export enum Orientation {
  /** Left direction */
  Left = 'left',
  /** Right direction */
  Right = 'right',
  /** Up direction */
  Up = 'up',
  /** Down direction */
  Down = 'down',
}
